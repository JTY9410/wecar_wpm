# 정밀 요구사항 정의서 및 개발 에이전트 지침서 (요구사항.md)

본 문서는 `/Users/USER/dev/car` 경로의 애플리케이션 프레임워크와 유기적으로 결합하여, **주 단위 경매 도매 시세 분석 엔진**과 **실시간 매물 동기화 시스템**을 구축하기 위한 **단일 진실 공급원(SSOT)** 기준의 정밀 요구사항 정의서(PRD) 및 AI 에이전트(Cursor / Claude) 개발 지침서입니다.

---

## ⚠️ [CRITICAL AGENT DIRECTION - 필수 준수 지침]

1. **SSOT(Single Source of Truth) 원칙 엄수**  
   * 모든 시스템 설정, 초기 관리자 계정 정보, API 엔드포인트, 경로, 속성값은 오직 프로젝트 루트의 `config.py`와 `.env`에서만 관리합니다. 
   * 코드베이스 내 어떠한 하드코딩(상수, 경로, API 키 등)도 허용하지 않으며, 발견 즉시 리팩토링하십시오.
2. **2-Tier 하이브리드 AI 구조 준수**  
   * **Tier 1 (정량적 가격 예측 및 시세 분석)**: 로컬 내부 무료 머신러닝 엔진(`Scikit-learn / RandomForestRegressor`)이 전담합니다.
   * **Tier 2 (부수적 외부 제미나이 AI)**: 구글 제미나이(`Gemini API`)는 부수적인 정성적 분석(소견 리포트 생성)에만 제한적으로 사용합니다. **제미나이가 가격 수치 자체를 직접 예측 및 계산하도록 만들지 마십시오.**
3. **3단계 반복 검증 루틴 의무 수행**  
   * 코드 수정 및 기능 구현 완료 후 다음 3단계 루틴을 최소 3회 이상 반복 검증하고 그 결과를 에이전트 브리핑 시 보고하십시오.
     * **1단계 (기능 만족도 확인)**: 폴백 전략, 이미지 로컬 캐싱, 1.5만km 구간화 기능이 정확히 동작하는가?
     * **2단계 (권한 분리 보안 체크)**: 일반 사용자가 URL 직접 입력 등으로 관리자 전용 API에 진입할 수 없는가?
     * **3단계 (예외 처리/안정성 테스트)**: 네트워크 타임아웃, 업로드 파일 훼손, API 키 만료 시 시스템이 토스트 알림 등으로 부드럽게 예외 복구되는가?

---

## 1. 기술 스택 및 실행 환경 (Tech Stack)

* **[BACKEND]** Python 3.11 / Flask 3.x / Flask-Migrate (Alembic 기반 DB 형상관리)
* **[DATABASE]** SQLite 3 (`instance/wecarcar1_auto.db` 경로 고정)
* **[INFRA]** Docker / Docker Compose (DB 및 로컬 업로드 파일 보존을 위한 볼륨 마운트 `instance:/app/instance` 필수 설정)
* **[FRONTEND]** HTML5 / CSS3 / Vanilla JavaScript (ES6+) / Bootstrap 5
* **[UI STYLE]** Dribbble 스타일 (둥근 모서리 `border-radius: 12px`, 부드러운 그림자 `box-shadow: 0 4px 20px rgba(0,0,0,0.05)`, 최소 디자인 테두리, 스켈레톤 로딩 구현)

---

## 2. 환경 변수 및 전역 설정 명세 (SSOT Config)

### 2.1. `.env` 환경 변수 구성 프로토타입
```env
FLASK_ENV=development
SECRET_KEY=ks_ai_secure_secret_key_2026
KS_API_BASE_URL=https://ks-auto-api.nodeplug.com/kindsisters
FALLBACK_API_BASE_URL=https://extapi.carmanager.co.kr
GEMINI_API_KEY=AIzaSyYourActualGeminiKeyHere
DATABASE_URL=sqlite:///instance/wecarcar1_auto.db
IMAGE_STORAGE_PATH=./instance/storage/car_images
EXCEL_UPLOAD_PATH=./instance/storage/excel_uploads

# [초기 최고 관리자 시드 계정 정보]
INIT_ADMIN_USERNAME=wecar
INIT_ADMIN_PASSWORD=1004wecar
```

### 2.2. `config.py` 싱글톤 정의 역할
* 프로젝트 기동 시 `.env` 파일을 완벽히 파싱하여 Config 싱글톤 객체를 초기화합니다.
* 초기 데이터베이스 생성 또는 마이그레이션 실행 시, `.env`에 정의된 `INIT_ADMIN_USERNAME` 및 `INIT_ADMIN_PASSWORD` 환경 변수를 안전하게 단방향 해싱하여 최고 관리자 계정을 데이터베이스에 자동으로 시딩(Seeding)해야 합니다.
* 내부 비즈니스 제약 속성값 정의:
  * `RATE_LIMIT_DAILY = 20` (일간 사용자 제미나이 리포트 요청 제한)
  * `SYNC_HOURS = [9, 13, 18]` (자동 동기화 주기 시간 타겟팅)

---

## 3. 데이터베이스 스키마 및 정제 규칙

모든 데이터베이스 변경은 Alembic 마이그레이션 도구(`flask db migrate / upgrade`)를 통해 추적 관리합니다. 원본 대문자/파스칼케이스 데이터는 아래 정의된 소문자 스네이크케이스(`snake_case`) 모델에 1:1 매핑되어 저장됩니다.

```
                  ┌──────────────┐ (1)
                  │     User     │───┐
                  └──────────────┘   │
                                     │ (N)
                                     ▼
                          ┌──────────────────────┐
                          │  GeminiReportCache   │
                          └──────────────────────┘
                                     ▲
                                     │ (1)
                                     │ (1)
                  ┌──────────────────────┐
                  │       Listing        │
                  └──────────────────────┘
```

### 3.1. 핵심 모델 세부 명세

#### `User` (사용자 계정 및 역할 분담)
* `id` (INTEGER, Primary Key, Auto-Increment)
* `username` (VARCHAR(80), Unique, Not Null) -> **초기 자동 인젝션 타겟: 'wecar'**
* `password_hash` (VARCHAR(255), Not Null) -> **초기 자동 인젝션 타겟: '1004wecar' 단방향 해싱 값**
* `role` (VARCHAR(20), Default: 'USER') - `'USER'`(일반 고객/딜러) 또는 `'ADMIN'`(관리자)

#### `Listing` (차량 매물 누적 레이크)
* `car_no` (VARCHAR(50), Primary Key) - 원본 `CarNo` 매핑
* `car_name` (VARCHAR(150)) - 원본 `CarName` 매핑
* `maker_no` (INTEGER) - 원본 `MakerNo` 매핑
* `car_year` (INTEGER) - 원본 `CarYear` 매핑
* `car_km` (INTEGER) - 원본 `CarKm` 매핑
* `car_amount_sale` (INTEGER) - 원본 `CarAmountSale` (만원 단위)
* `sido` (VARCHAR(50)) - 원본 `Sido` 매핑
* `kind_name` (VARCHAR(100)) - 원본 `KindName` 매핑
* `imported` (VARCHAR(5)) - 원본 `Imported` ('Y' 또는 'N')
* `is_sold` (BOOLEAN, Default: False) - **[중요]** 매주 실행되는 동기화 API 결과 리스트에서 현재 매물 번호가 누락되어 있는 경우, 판매 완료된 매물로 인지하여 `True`로 자동 변경하되 AI 자체 통계 학습용 기초 데이터로 영구 보존함.
* `updated_at` (DATETIME, Default: CURRENT_TIMESTAMP)

#### `GeminiReportCache` (정성 분석 리포트 캐싱용)
* `car_no` (VARCHAR(50), Primary Key, ForeignKey('listing.car_no'))
* `report_text` (TEXT) - 생성된 다국어 지원 리포트 문단
* `created_at` (DATETIME, Default: CURRENT_TIMESTAMP)

#### `SyncLog` (동기화 및 AI 재학습 이력 로그)
* `id` (INTEGER, PK)
* `sync_type` (VARCHAR(50)) - `'WEEKLY_EXCEL_UPLOAD'` 또는 `'AUTO_API_SYNC'`
* `status` (VARCHAR(20)) - `'SUCCESS'`, `'FAIL'`
* `records_processed` (INTEGER) - 영향받은 데이터 수
* `error_message` (TEXT, Nullable)
* `created_at` (DATETIME, Default: CURRENT_TIMESTAMP)

---

## 4. 엑셀 업로드 시스템 상세 설계 (Excel Upload System)

매주 주기적으로 업로드되는 주 단위 경매 데이터를 안정적으로 처리하고 관리하기 위한 전용 파이프라인을 구축합니다.

### 4.1. 엑셀 업로드 페이지 UI/UX 구성
* **드롭존(Drag & Drop)**: 사용자가 엑셀 파일(`.xlsx`, `.xls`)을 드래그 앤 드롭으로 업로드할 수 있는 모던 UI 컴포넌트 제공.
* **주간 회차/기준일 지정**: 
  * 데이터가 적용될 주차(예: 2026년 29주차) 및 기준 경매일을 관리자가 선택하거나 파일명에서 정규식으로 자동 추출하도록 유도.
* **데이터 갱신 방식 선택 (라디오 버튼)**:
  1. `주간 데이터 추가(Append)`: 기존 DB에 해당 주차의 낙찰 데이터를 누적함. (기본값)
  2. `해당 주차 덮어쓰기(Overwrite)`: 동일 주차 데이터 존재 시 에러 방지를 위해 삭제 후 다시 덮어씀.
  3. `전체 초기화 후 등록(Reset & Reload)`: 기존 모든 분석 테이블 데이터를 지우고 전면 재업로드.
* **업로드 히스토리 테이블**: 역대 엑셀 업로드 이력 표기 (업로드 일시, 파일명, 처리상태, 성공 행 수, 작업자 계정).

### 4.2. 백엔드 유효성 검증 (Validation) 파이프라인
* **필수 컬럼 스키마 검증**: `경매일`, `제조사`, `차명`, `연식`, `주행거리`, `낙찰가`, `내수수출구분` 등의 컬럼 존재를 엄격하게 확인합니다.
* **데이터 정제 캐스팅**:
  * `주행거리` 내 비수치형 데이터(예: "미상", "확인불가")는 에러 처리 대신 `NaN` 또는 `0`으로 자동 클렌징합니다.
  * `낙찰가` 정보가 누락된 행은 분석 대상에서 자동 제외 처리합니다.

---

## 5. 백엔드 핵심 비즈니스 로직 및 전처리 규칙 (Data Pipeline)

### 5.1. 카코드(Car-Code) 체계 정규화 룰
* **기준 데이터**: `시세표_테이블` 시트의 `[제조사, 차명, 연료, 내수/수출구분]` 고유 조합.
* **매핑 메커니즘**:
  * `경매전체데이터`의 `제조사` 및 `차명` 문자열의 공백과 특수문자를 제거(Trimming)한 후 1차 매칭을 수행합니다.
  * 비정형 차명 문자열 처리를 위해 **Levenshtein Distance(편집 거리)** 알고리즘 스코어 0.85 이상인 대상을 식별하여 표준 카코드를 다이내믹하게 매핑 및 부여합니다.

### 5.2. 주행거리 15,000km 단위 구간화 (Binning) 파이썬 구현 표준
* 시세 데이터를 주행거리 기반 범주로 정렬하기 위한 핵심 연산식입니다.
* **구간화 로직**:
  * $	ext{주행거리} \ge 200,000 
ightarrow 	ext{"20만km 이상"}$
  * 그 외: $\lfloor 	ext{주행거리} / 15000 
floor 	imes 15000$ 공식을 적용하여 `[하한값]만 ~ [상한값]만km` 형태로 범주화 레이블링합니다.

### 5.3. 무사고 여부 및 시세 계산 공식
* **무사고 판정**: `사고'A' 상세` 컬럼이 '무사고'이거나 교환 이력(`XX 교환 `) 및 판금 이력(`W 판금,용접,꺾임`) 데이터가 모두 `0` 또는 `NaN`인 차량을 무사고 표준 데이터셋으로 분류합니다.
* **전달평균대비비교(MoM) 변동률**:
  $$	ext{변동률(\%)} = \left( rac{	ext{당월 낙찰가 평균} - 	ext{전월 낙찰가 평균}}{	ext{전월 낙찰가 평균}} 
ight) 	imes 100$$

### 5.4. 폴백(Fallback) 엔진 기반 동기화 스케줄러
* **동작 트리거**: 매일 09:00, 13:00, 18:00 KST 정적 시간대 또는 어드민 수동 동기화 요청 시 구동.
* **통신 규칙**: `KS_API_BASE_URL/listings` API 호출 시 타임아웃 제한 시간 `timeout=120`초 설정.
* **예외 복구(Fallback)**: API 장애(5xx, 4xx, 타임아웃 등) 발생 시 즉시 `FALLBACK_API_BASE_URL`로 백업 엔드포인트를 전향(Fallback) 처리하여 데이터를 정상 파싱하고 통계 단절을 미연에 방지합니다.

### 5.5. 로컬 이미지 보안 캐싱 (Image Safeguard)
* 외부 도메인의 이미지 주소(`ImageUrl`)를 클라이언트 프론트엔드 단에 직접 표출하는 행위를 보안상 엄격히 금지합니다.
* 백엔드 API 동기화 시점에 `ImageInfo[0].ImageUrl` 원본을 백엔드 비동기 세션으로 가져와 로컬 세이프 스토리지 (`IMAGE_STORAGE_PATH`) 경로에 `{car_no}_main.jpg` 파일명으로 보존 처리합니다.
* 프론트엔드 렌더링 시에는 우회 경로인 `/static/storage/car_images/{car_no}_main.jpg` 형태의 고유 가상화 로컬 URL을 제공합니다.

### 5.6. 2-Tier 하이브리드 AI 파이프라인
* **Tier 1 (로컬 무료 ML 예측)**
  * 기술: Scikit-learn 패키지의 `RandomForestRegressor` 모델을 사용합니다.
  * 학습 변수: 독립변수 $X$ = `[maker_no, car_year, car_km, imported]` (범주형 데이터는 라벨 인코딩 수치화), 종속변수 $y$ = `car_amount_sale`.
  * 재학습 트리거: 주간 엑셀 업로드 또는 주기적 API 동기화가 끝난 직후, 전체 축적된 데이터 레이크를 피팅(`model.fit()`)하여 `car_price_model.pkl` 정적 모델 파일로 오버라이트 덤프를 진행합니다.
* **Tier 2 (부수적 외부 제미나이 정성 소견)**
  * 기술: 구글 `google-genai` 혹은 REST API 규격의 `gemini-2.5` 계열 사용.
  * 작동 조건: 상세 화면에서 사용자가 [AI 정성 분석 리포트 발행] 클릭 시에만 제한적으로 활성화.
  * 최적화: 불필요한 API 토큰 낭비를 막기 위해 생성 완료된 정성 리포트 결과물은 즉각 `GeminiReportCache` 테이블에 저장하여 중복 조회를 완벽하게 방지합니다.

---

## 6. UI/UX 및 권한 분리(RBAC) 및 글로벌 대응

### 6.1. 단계별 계층 검색 및 시세 데이터 표시 그리드 테이블
* **상단 조건 필터 영역 (Cascading Search)**: 
  1. `제조사` $
ightarrow$ 2. `차명` $
ightarrow$ 3. `세부등급` $
ightarrow$ 4. `년도(연식)`의 계층형 순차 필터링을 구축합니다.
* **시세 표시 그리드 테이블 사양**:
  상단 계층형 조건 필터를 기반으로 검색된 매물 분석 결과를 아래 필드를 포함하여 동적 그리드로 시각화합니다:
  * `카코드` / `최초등록년도` / `수출/내수 구분` / `무사고유무` / `키로수(15,000km 단위 구간)` / `시작가평균` / `낙찰가평균` / `전달평균대비비교(MoM %)` / `비고(및 수동 분석 실행 작업)`

### 6.2. 권한별 인터페이스 통제 (RBAC)
* **일반 사용자 (`role == 'USER'`)**:
  * 매물 계층형 조건 검색, 자체 AI 정량 예측가 확인, 제미나이 정성 리포트 리딩 기능 제공.
  * Jinja2 구문을 통해 시스템 수동 스케줄링 제어판, 시스템 자원 분석 카드 등 어드민 관련 제어 버튼을 DOM 구조상에서 완전 배제합니다.
  * URL 강제 주소창 접근 시 403 Forbidden 및 접근 거부 알림 팝업 전송.
* **시스템 관리자 (`role == 'ADMIN'`)**:
  * **초기 마이그레이션 및 시딩 완료된 `wecar` / `1004wecar` 인증 계정을 통해 로그인 가동.**
  * 시스템 어드민 대시보드 접근 권한 활성화.
  * `[수동 주간 엑셀 파싱 및 데이터 분석]`, `[AI 모델 강제 수동 재학습]` 제어 인터페이스 표출.
  * 모든 어드민 대상 API 라우트는 백엔드 데코레이터 `@admin_required`를 선언하여 강제 제어합니다.

### 6.3. 글로벌 다국어 자동 번역 및 번역 캐싱
* **i18n 언어팩 아키텍처**: 프론트엔드 다국어 번역 리소스 구축 (`ko.json`, `en.json`, `ja.json`).
* **실시간 번역 엔진**: `경매팀사고내용`, `기능평가상세`와 같은 비정형 데이터 텍스트는 번역 API(DeepL / AWS Translate)를 통해 영문 및 일문으로 실시간 자동 번역합니다.
* **로컬 번역 캐시 풀**: 한 번 번역된 원문 텍스트는 로컬 번역 캐싱 테이블에 저장하여 번역 API 호출 요금 및 대기 시간을 절약합니다.

---

## 7. 어드민 AI 제어 및 로컬 자율 학습 (Self-Learning) 엔진 요건

### 7.1. 멀티 LLM 플러그인 스위칭 허브
* 어드민 UI 상에 **[OpenAI ChatGPT]**, **[Google Gemini]**, **[Anthropic Claude]** API 설정 패널을 구현합니다.
* 토글 조작만으로 실시간 시세 요약 리포트를 발행하는 백엔드 LLM 엔진 인터페이스를 동적 스위칭(Strategy Pattern) 가능하도록 설계합니다.

### 7.2. 인하우스 로컬 자율 학습 (Self-Learning) 구현
외부 API의 의존성을 대폭 낮추고 보안 및 연산 비용을 최적화하기 위해, 로컬에서 주기적으로 구동되는 **자율 학습 파이프라인**을 확보합니다.
1. **실시간 데이터 임베딩 및 RAG**:
   * 매주 새롭게 업로드되는 `경매전체데이터` 시트 내 차량 진단 상세 데이터를 오픈소스 임베딩 모델(`SentenceTransformers/all-MiniLM-L6-v2`)을 통해 고차원 벡터로 변환합니다.
   * 서버 내부 Vector DB(`Chroma` 또는 `pgvector`)에 실시간 적재하여 자연어 질의 시 초고속 주간 시세 매칭을 수행합니다.
2. **자율 피드백 기반 점진적 강화 학습**:
   * 경매 데이터가 매주 누적될 때마다 딜러 오퍼레이션 피드백 로그를 보상 함수(Reward Function)로 산정합니다.
   * 로컬 서버 백그라운드에서 오픈소스 소형 LLM(예: `Llama-3-8B-Instruct`)을 대상으로 하는 QLoRA 기반 미세조정(Fine-Tuning) 스크립트를 자동 배치 형태로 구동하여 시스템이 자체적으로 최신 도매 시세 트렌드를 학습하도록 구현합니다.

---

## 8. `/Users/USER/dev/car` 백엔드 핵심 파이썬 스크립트 (주간 업로드 및 가공)

개발 환경에서 즉시 구동 및 API로 노출할 수 있는 주간 단위 파일 업로드 및 데이터 파싱 연산 통합 스크립트입니다.

```python
import os
import pandas as pd
import numpy as np
import json
from datetime import datetime

class WeeklyAuctionDataPipeline:
    def __init__(self, db_destination="/Users/USER/dev/car/data/processed_market_price.json"):
        self.db_destination = db_destination
        os.makedirs(os.path.dirname(self.db_destination), exist_ok=True)

    def validate_excel_schema(self, df):
        """업로드된 엑셀 파일의 필수 컬럼 유효성 검증"""
        required_cols = ['경매일', '제조사', '모델명', '차명', '연식', '주행거리', '시작가(만원)', '낙찰가', '내수수출구분']
        missing_cols = [col for col in required_cols if col not in df.columns]
        if missing_cols:
            raise ValueError(f"필수 컬럼이 누락되었습니다: {missing_cols}")
        return True

    def calculate_mileage_bin(self, mileage):
        """주행거리 15,000km 단위 구간화 (20만 이상 최종 표기)"""
        if pd.isna(mileage): return "미상"
        mileage = float(mileage)
        if mileage >= 200000: return "20만km 이상"
        low = int((mileage // 15000) * 15000)
        high = low + 15000
        return f"{low//10000:.1f}만~{high//10000:.1f}만km"

    def process_weekly_upload(self, file_path, upload_mode="append"):
        """
        매주 업로드되는 엑셀 가공 실행 함수
        upload_mode: 'append' (기존 데이터에 추가) 또는 'overwrite' (전체 초기화 후 신규 생성)
        """
        xls = pd.ExcelFile(file_path)
        df_new = pd.read_excel(xls, sheet_name='경매전체데이터')
        
        # 1. 스키마 유효성 검사
        self.validate_excel_schema(df_new)
        
        # 2. 데이터 정제 및 가공
        df_new['주행거리_구간'] = df_new['주행거리'].apply(self.calculate_mileage_bin)
        
        # 무사고 유무 판정 (교환 'XX 교환 ', 판금 'W 판금,용접,꺾임' 컬럼 기준)
        xx_col = [c for c in df_new.columns if 'XX 교환' in c][0]
        w_col = [c for c in df_new.columns if 'W 판금' in c][0]
        
        df_new['무사고유무'] = np.where(
            (df_new["사고'A' 상세"] == '무사고') | 
            ((df_new[xx_col].fillna(0) == 0) & (df_new[w_col].fillna(0) == 0)), 
            '무사고', '사고차'
        )
        
        # 3. 그룹바이를 통한 최종 요약 분석 테이블 생성
        summary = df_new.groupby(['제작사', '모델명', '연식', '내수수출구분', '무사고유무', '주행거리_구간']).agg(
            최초등록년도_샘플=('연식', 'first'),
            시작가_평균=('시작가(만원)', 'mean'),
            낙찰가_평균=('낙찰가', 'mean'),
            데이터_건수=('낙찰가', 'count')
        ).reset_index()
        
        # 가상의 전주/전월 대비 시세 등락폭 연산 (+- 1.5% 트렌드 기준 데이터 생성)
        summary['전달평균대비비교'] = np.round(summary['낙찰가_평균'] * 0.015, 2)
        summary['비고'] = "주간 업로드 완료 (" + datetime.now().strftime("%Y-%m-%d") + ")"
        
        # 4. 모드별 DB 저장 처리
        processed_data = summary.to_dict(orient='records')
        
        if upload_mode == "append" and os.path.exists(self.db_destination):
            with open(self.db_destination, 'r', encoding='utf-8') as f:
                existing_data = json.load(f)
            combined_data = existing_data + processed_data
        else:
            combined_data = processed_data
            
        with open(self.db_destination, 'w', encoding='utf-8') as f:
            json.dump(combined_data, f, ensure_ascii=False, indent=4)
            
        return len(combined_data)

if __name__ == "__main__":
    pipeline = WeeklyAuctionDataPipeline()
    # 신규 엑셀 업로드 시뮬레이션
    total_records = pipeline.process_weekly_upload("20260715_낙찰데이터_최종_최종_최종.xlsx", upload_mode="overwrite")
    print(f"데이터베이스 갱신 완료. 총 분석 레코드 수: {total_records}개")
```

---

## 9. 에이전트 품질 제어 루틴 (3-Step Verification Guidelines)

본 지침을 수행하는 Cursor/Claude AI 개발 에이전트는 코드 수정 사항 배포 및 기능 테스트 보고 시 하단의 3가지 품질 제어 프로세스를 완벽히 완료했음을 본문에 상세 로그를 통해 검증 증명해야 합니다.

```
[1단계: 요구사항 만족 검증] ──➔ [2단계: 우회 취약점 차단 검증] ──➔ [3단계: 시스템 예외 복구력 검증]
```

1. **[1단계]** `config.py` 싱글톤 기반의 무하드코딩 규칙, 폴백 기반의 주간 동기화 스케줄러 오퍼레이션, 로컬 우회 이미지 서빙 캐싱 레이어가 빈틈없이 프로그래밍되었는지 데이터 흐름을 점검하십시오.
2. **[2단계]** 클라이언트 단에서 어드민 전용 URL(`/admin/sync/trigger` 등)을 세션 탈취 및 임의 가로채기를 통해 요청을 보낼 시 백엔드 데코레이터에서 세션을 정상 탐지하여 가로막는지 침투 테스트 관점으로 검토하십시오.
3. **[3단계]** 구글 제미나이 API가 만료되거나 일일 할당 트래픽 제한(Quota)을 초과한 비상 상황에서도, 로컬 Tier 1 예측 모델은 정상적으로 오프라인 상태에서 동작하며 사용자에게 프론트엔드 토스트 창을 통해 "임시 리포트 생성 불가능"을 알리는 그레이스풀 폴백 예외처리가 되었는지 무한루프 테스트를 수행하십시오.
